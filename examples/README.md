# WareHouse locator - Examples

To run our examples Python needs to be installed and dependencies installed with:
```
$ pip install -r requirements.txt
```
Then each can be run with:
```
$ python <example-file>
```